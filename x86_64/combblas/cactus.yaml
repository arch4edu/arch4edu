nvchecker:
  - source: aur
    aur:
build_prefix: extra-x86_64
pre_build: |
  aur-pre-build
  sed -i 's/-DBUILD_TESTING=OFF/-DBUILD_TESTING=OFF \\\n    -DCMAKE_POLICY_VERSION_MINIMUM=3.5/' PKGBUILD
post_build: aur-post-build
