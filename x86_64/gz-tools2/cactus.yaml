nvchecker:
  - source: aur
    aur:
makedepends:
  - any/gz-cmake3
build_prefix: extra-x86_64
pre_build: |
  aur-pre-build
  add depends ruby gcc-libs glibc
  sed -i "s/^arch=('any')$/arch=('x86_64')/" PKGBUILD
post_build: aur-post-build
