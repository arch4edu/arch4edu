nvchecker:
  - source: aur
    aur:
depends:
  - x86_64/glib2-static
  - x86_64/pcre-static
build_prefix: extra-x86_64
pre_build:
  - aur-pre-build
  - sudo sed 's/nproc/echo 2/' -i /etc/makepkg.conf
post_build: aur-post-build
