nvchecker:
  - source: aur
    aur:
depends:
  - x86_64/ros2/ros2-jazzy
build_prefix: extra-x86_64
pre_build: |
  aur-pre-build
  replace -u '5.5.0' '5.9.0'
  replace "arch=('any')" "arch=('x86_64')"
post_build: aur-post-build
