nvchecker:
  - source: aur
    aur:
depends:
  - x86_64/kokkos
  - x86_64/xeus-zmq
build_prefix: extra-x86_64
pre_build: |
  aur-pre-build
  # The test_asr_* ctest cases import toml, but upstream declares no
  # checkdepends at all.
  add checkdepends python-toml
post_build: aur-post-build
