nvchecker:
  - source: aur
    aur:
depends:
  - x86_64/rocm/hip-runtime-amd
makedepends:
  - x86_64/rocm/rocm-cmake
group: x86_64
build_prefix: extra-x86_64
pre_build: |
  aur-pre-build
  sed 's/$(nproc)/1/' -i PKGBUILD
  set-swap 40G
post_build: |
  aur-post-build
  set-swap
