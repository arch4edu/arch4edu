nvchecker:
  - source: aur
    aur:
  - alias: opencv
depends:
  - x86_64/opencv4
build_prefix: extra-x86_64
pre_build: |
  aur-pre-build
  sed -i "s/'opencv'/'opencv4'/" PKGBUILD
  echo 'export CXXFLAGS+=" -I/usr/include/opencv4"' >> PKGBUILD
post_build: aur-post-build
