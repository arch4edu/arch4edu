nvchecker:
  - source: aur
    aur:
depends:
  - x86_64/udunits
build_prefix: extra-x86_64
pre_build: |
  aur-pre-build
  sed -i 's/-Wno-incompatible-pointer-types/-Wno-incompatible-pointer-types -std=gnu17/' PKGBUILD
post_build: aur-post-build
