nvchecker:
  - source: aur
    aur:
build_prefix: extra-x86_64
pre_build: |
  aur-pre-build
  add makedepends openmpi
  sed -i 's|cmake ../opencv-\$pkgver|cmake ../opencv-$pkgver -DCMAKE_POLICY_VERSION_MINIMUM=3.5|' PKGBUILD
post_build: aur-post-build
