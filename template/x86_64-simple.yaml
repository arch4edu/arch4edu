nvchecker:
  - source: aur
    aur:
  - source: archpkg
    archpkg: gdal
    from_pattern: '^(\d+\.\d+)\..*'
    to_pattern: '\1'
build_prefix: extra-x86_64
pre_build: aur-pre-build
post_build: aur-post-build
