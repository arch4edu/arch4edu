nvchecker:
  - source: aur
    aur:
depends:
  - any/pnpm
build_prefix: extra-aarch64
makepkg_args: -A
pre_build: |
  aur-pre-build
  add makedepends nodejs-lts-gallium
  sed 's/pkgver()/_pkgver()/' -i PKGBUILD
post_build: aur-post-build
